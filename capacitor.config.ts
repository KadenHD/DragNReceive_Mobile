import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dragnreceive_mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
